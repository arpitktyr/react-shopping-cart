import store from "../../redux/store";

describe("Product  must be zero at beginning", () => {
  it("Should initially set games to an empty object", () => {
    // const state = store.getState().product;
    // expect(state.product).toEqual({});
  });
});
