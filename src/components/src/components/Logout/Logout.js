import React from 'react';

import Login from '../Login/Login';

const Logout = (props) => {
    return (
        <div>
            <Login />
        </div>
    )
}

export default Logout
